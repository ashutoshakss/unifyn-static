# Cloudflare Pages redirects and rewrites
# https://developers.cloudflare.com/pages/configuration/redirects/

# Serve index.html at root
/ /index.html 200

# Redirect old URLs (if you had any legacy structure)
/home /index.html 301
/home.html /index.html 301

# Redirect login/signup pages to main page with hash (since we use modals now)
# Optional: comment out if you want to keep standalone pages
# /login.html /#login 301
# /signup.html /#signup 301

# Ensure trailing slashes are handled consistently
/contact /contact.html 301
/terms /terms.html 301
/privacy /privacy.html 301

# Force HTTPS (Cloudflare Pages handles this by default, but explicit is good)
# http://* https://:splat 301

# Redirect www to non-www (or vice versa)
# https://www.unifyn.trade/* https://unifyn.trade/:splat 301

# Handle 404s - serve custom 404 page (must be last)
/* /404.html 404

# Security headers (optional - can also be set in cloudflare.toml or dashboard)
/*
  X-Frame-Options: DENY
  X-Content-Type-Options: nosniff
  X-XSS-Protection: 1; mode=block
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: geolocation=(), microphone=(), camera=()

